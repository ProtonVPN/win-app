<Page
    x:Class="ProtonVPN.Gui.Views.Pages.CountriesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:models="using:ProtonVPN.Gui.Models"
    mc:Ignorable="d"
    x:Name="PageArea">

    <Grid x:Name="ContentArea">
        <ItemsControl ItemsSource="{x:Bind ViewModel.Countries}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Vertical" Spacing="8"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate x:DataType="models:Country">
                    <Button Command="{Binding ViewModel.NavigateToCountryCommand, ElementName=PageArea}"
                            CommandParameter="{x:Bind }"
                            Content="{x:Bind CountryName}"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</Page>
