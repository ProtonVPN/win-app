<?xml version="1.0" encoding="utf-8" ?>
<!--
Copyright (c) 2025 Proton AG

This file is part of ProtonVPN.

ProtonVPN is free software: you can redistribute it and/or modify
it under the terms of the GNU General Public License as published by
the Free Software Foundation, either version 3 of the License, or
(at your option) any later version.

ProtonVPN is distributed in the hope that it will be useful,
but WITHOUT ANY WARRANTY; without even the implied warranty of
MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
GNU General Public License for more details.

You should have received a copy of the GNU General Public License
along with ProtonVPN.  If not, see <https://www.gnu.org/licenses />.-->

<Page x:Class="ProtonVPN.Client.UI.Main.Settings.Connection.SplitTunnelingPageView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:controls="using:CommunityToolkit.WinUI.Controls"
      xmlns:pathicons="using:ProtonVPN.Client.Common.UI.Assets.Icons.PathIcons"
      xmlns:custom="using:ProtonVPN.Client.Common.UI.Controls.Custom"
      xmlns:models="using:ProtonVPN.Client.Core.Models"
      xmlns:featureicons="using:ProtonVPN.Client.UI.Main.FeatureIcons"
      xmlns:networking="using:ProtonVPN.Common.Core.Networking"
      x:DefaultBindMode="OneWay"
      AutomationProperties.AutomationId="SplitTunnelingPage"
      AutomationProperties.Name="{x:Bind ViewModel.Localizer.Get('Automation_Properties_SplitTunneling_Page')}"
      NavigationCacheMode="Required"
      mc:Ignorable="d">

    <Page.Resources>
        <Style x:Key="SplitTunnelingRadioButtonStyle" BasedOn="{StaticResource RadioButtonStyle}" TargetType="RadioButton">
            <Setter Property="GroupName" Value="SplitTunnelingMode" />
        </Style>

        <Thickness x:Key="PageContentControlPadding">16</Thickness>

        <DataTemplate x:Key="Ipv6EnabledNetworkAddressTemplate"
                      x:DataType="networking:NetworkAddress">
            <Border Padding="8,2"
                    Background="{ThemeResource InteractionDefaultColorBrush}"
                    CornerRadius="4">
                <TextBlock Style="{StaticResource BodyTextBlockStyle}"
                           Text="{x:Bind}"
                           TextTrimming="CharacterEllipsis"
                           TextWrapping="NoWrap" />
            </Border>
        </DataTemplate>

        <DataTemplate x:Key="Ipv6DisabledNetworkAddressTemplate"
                      x:DataType="networking:NetworkAddress">
            <Border Padding="8,2"
                    Background="{ThemeResource InteractionDefaultColorBrush}"
                    CornerRadius="4">
                <TextBlock Opacity="{x:Bind IsIpV6, Converter={StaticResource BooleanToReducedOpacityConverter}}"
                           Style="{StaticResource BodyTextBlockStyle}"
                           Text="{x:Bind}"
                           TextTrimming="CharacterEllipsis"
                           TextWrapping="NoWrap" />
            </Border>
        </DataTemplate>
    </Page.Resources>

    <custom:SettingsPageContentControl x:Name="PageContentHost"
                                       ApplyCommand="{x:Bind ViewModel.ApplyCommand, Mode=OneTime}"
                                       ApplyCommandText="{x:Bind ViewModel.ApplyCommandText}"
                                       BackCommand="{x:Bind ViewModel.NavigateBackCommand, Mode=OneTime}"
                                       CloseCommand="{x:Bind ViewModel.CloseCommand, Mode=OneTime}"
                                       IsBackButtonVisible="{x:Bind ViewModel.CanGoBack}">

        <custom:SettingsPageContentControl.PageHeader>
            <custom:SettingsPageHeaderContentControl Header="{x:Bind ViewModel.Title}">
                <custom:SettingsPageHeaderContentControl.Illustration>
                    <featureicons:SplitTunnelingIconView />
                </custom:SettingsPageHeaderContentControl.Illustration>
            </custom:SettingsPageHeaderContentControl>
        </custom:SettingsPageContentControl.PageHeader>

        <StackPanel x:Name="ContentArea"
                    Spacing="24">

            <controls:SettingsExpander Header="{x:Bind ViewModel.Localizer.Get('Settings_Connection_SplitTunneling')}"
                                       IsExpanded="{x:Bind ViewModel.IsSplitTunnelingEnabled}"
                                       IsTabStop="False">

                <controls:SettingsExpander.Description>
                    <TextBlock TextWrapping="Wrap">
                        <Run Text="{x:Bind ViewModel.Localizer.Get('Settings_Connection_SplitTunneling_Description')}" />
                        <Hyperlink NavigateUri="{x:Bind ViewModel.LearnMoreUrl, Mode=OneTime}"
                                   ToolTipService.ToolTip="{x:Bind ViewModel.LearnMoreUrl, Mode=OneTime}">
                            <Run Text="{x:Bind ViewModel.Localizer.Get('Common_Links_LearnMore')}" />
                        </Hyperlink>
                        <Run />
                    </TextBlock>
                </controls:SettingsExpander.Description>

                <ToggleSwitch AutomationProperties.AutomationId="SplitTunnelingSwitch"
                              IsOn="{x:Bind ViewModel.IsSplitTunnelingEnabled, Mode=TwoWay}"
                              Style="{StaticResource ToggleSwitchStyle}" />
                <controls:SettingsExpander.Items>
                    <StackPanel Spacing="16">
                        <RadioButton AutomationProperties.AutomationId="ExcludeModeRadioButton"
                                     IsChecked="{x:Bind ViewModel.IsStandardSplitTunneling, Mode=TwoWay}"
                                     Style="{StaticResource SplitTunnelingRadioButtonStyle}">
                            <StackPanel>
                                <TextBlock Style="{StaticResource BodyTextBlockStyle}"
                                           Text="{x:Bind ViewModel.Localizer.Get('Settings_Connection_SplitTunneling_Standard')}" />
                                <TextBlock Foreground="{ThemeResource TextWeakColorBrush}"
                                           Style="{StaticResource CaptionTextBlockStyle}"
                                           Text="{x:Bind ViewModel.Localizer.Get('Settings_Connection_SplitTunneling_Standard_Description')}"
                                           TextWrapping="Wrap" />
                            </StackPanel>
                        </RadioButton>
                        <RadioButton AutomationProperties.AutomationId="IncludeModeRadioButton"
                                     IsChecked="{x:Bind ViewModel.IsInverseSplitTunneling, Mode=TwoWay}"
                                     Style="{StaticResource SplitTunnelingRadioButtonStyle}">
                            <StackPanel>
                                <TextBlock Style="{StaticResource BodyTextBlockStyle}"
                                           Text="{x:Bind ViewModel.Localizer.Get('Settings_Connection_SplitTunneling_Inverse')}" />
                                <TextBlock Foreground="{ThemeResource TextWeakColorBrush}"
                                           Style="{StaticResource CaptionTextBlockStyle}"
                                           Text="{x:Bind ViewModel.Localizer.Get('Settings_Connection_SplitTunneling_Inverse_Description')}"
                                           TextWrapping="Wrap" />
                            </StackPanel>
                        </RadioButton>
                    </StackPanel>
                </controls:SettingsExpander.Items>
            </controls:SettingsExpander>

            <Grid Visibility="{x:Bind ViewModel.IsSplitTunnelingEnabled, Converter={StaticResource BooleanToVisibilityConverter}}">

                <ProgressRing Width="16"
                              Height="16"
                              IsActive="{x:Bind ViewModel.IsLoading}"
                              Visibility="{x:Bind ViewModel.IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}" />

                <StackPanel Spacing="8"
                            Visibility="{x:Bind ViewModel.IsLoading, Converter={StaticResource NotBooleanToVisibilityConverter}}">

                    <TextBlock Style="{StaticResource BodyTextBlockStyle}" 
                               Foreground="{ThemeResource TextWeakColorBrush}"
                               Text="{x:Bind ViewModel.AppsHeader}"
                               TextTrimming="CharacterEllipsis" />

                    <controls:SettingsCard AutomationProperties.AutomationId="AppsSelectorSettingsCard"
                                           Command="{x:Bind ViewModel.SelectAppsCommand, Mode=OneTime}"
                                           IsClickEnabled="True"
                                           Style="{StaticResource DefaultSettingsCardStyle}">

                        <controls:SettingsCard.Header>

                            <Grid>
                                <TextBlock Foreground="{ThemeResource TextHintColorBrush}"
                                           Text="{x:Bind ViewModel.Localizer.Get('Settings_Connection_SplitTunneling_Apps_Select')}"
                                           Visibility="{x:Bind ViewModel.HasSelectedApps, Converter={StaticResource NotBooleanToVisibilityConverter}}" />

                                <ScrollViewer MaxHeight="80"
                                              Style="{StaticResource OffsetScrollViewer}"
                                              VerticalScrollBarVisibility="Auto">
                                    <ItemsControl VerticalAlignment="Center"
                                                  ItemsSource="{x:Bind ViewModel.SelectedApps}"
                                                  Visibility="{x:Bind ViewModel.HasSelectedApps, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <controls:WrapPanel HorizontalSpacing="8"
                                                                    Orientation="Horizontal"
                                                                    VerticalSpacing="8" />
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate x:DataType="models:TunnelingApp">
                                                <Image Grid.Column="0"
                                                       Width="20"
                                                       Height="20"
                                                       Source="{x:Bind AppIcon}">
                                                    <ToolTipService.ToolTip>
                                                        <ToolTip>
                                                            <StackPanel Orientation="Vertical">
                                                                <TextBlock Text="{x:Bind AppName}" />
                                                                <TextBlock Foreground="{ThemeResource TextWeakColorBrush}"
                                                                           Text="{x:Bind AppPath}"
                                                                           TextTrimming="CharacterEllipsis" />
                                                            </StackPanel>
                                                        </ToolTip>
                                                    </ToolTipService.ToolTip>
                                                </Image>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </ScrollViewer>
                            </Grid>

                        </controls:SettingsCard.Header>

                        <controls:SettingsCard.ActionIcon>
                            <pathicons:ChevronRight />
                        </controls:SettingsCard.ActionIcon>
                    </controls:SettingsCard>

                    <Grid Margin="0,16,0,0"
                          HorizontalAlignment="Left">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0"
                                   Style="{StaticResource BodyTextBlockStyle}"
                                   Foreground="{ThemeResource TextWeakColorBrush}"
                                   Text="{x:Bind ViewModel.IpAddressesHeader}"
                                   TextTrimming="CharacterEllipsis" />

                        <custom:GhostButton Grid.Column="1"
                                            Margin="8,-8,0,-8"
                                            HorizontalAlignment="Left"
                                            AutomationProperties.AutomationId="ShowIpv6DisabledWarningButton"
                                            AutomationProperties.FullDescription="{x:Bind ViewModel.Localizer.Get('Overlay_Ipv6Disabled_Description')}"
                                            AutomationProperties.Name="{x:Bind ViewModel.Localizer.Get('Overlay_Ipv6Disabled_Title')}"
                                            Command="{x:Bind ViewModel.TriggerIpv6DisabledWarningCommand, Mode=OneTime}"
                                            ToolTipService.ToolTip="{x:Bind ViewModel.Localizer.Get('Overlay_Ipv6Disabled_Title')}"
                                            Visibility="{x:Bind ViewModel.HasIpv6AddressesWhileIpv6Disabled, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <custom:GhostButton.LeftIcon>
                                <pathicons:ExclamationTriangleFilled Foreground="{ThemeResource SignalWarningColorBrush}"
                                                                     Size="Pixels16" />
                            </custom:GhostButton.LeftIcon>
                        </custom:GhostButton>
                    </Grid>

                    <controls:SettingsCard AutomationProperties.AutomationId="IpAddressesSelectorSettingsCard"
                                           Command="{x:Bind ViewModel.SelectIpsCommand, Mode=OneTime}"
                                           IsClickEnabled="True"
                                           Style="{StaticResource DefaultSettingsCardStyle}">

                        <controls:SettingsCard.Header>

                            <Grid>
                                <TextBlock Foreground="{ThemeResource TextHintColorBrush}"
                                           Text="{x:Bind ViewModel.Localizer.Get('Settings_Connection_SplitTunneling_IpAddresses_Select')}"
                                           Visibility="{x:Bind ViewModel.HasSelectedIpAddresses, Converter={StaticResource NotBooleanToVisibilityConverter}}" />

                                <ScrollViewer MaxHeight="90"
                                              Style="{StaticResource OffsetScrollViewer}"
                                              VerticalScrollBarVisibility="Auto">
                                    <ItemsControl VerticalAlignment="Center"
                                                  ItemTemplate="{x:Bind SelectIpAddressItemTemplate(ViewModel.HasIpv6AddressesWhileIpv6Disabled)}"
                                                  ItemsSource="{x:Bind ViewModel.SelectedIpAddresses}"
                                                  Visibility="{x:Bind ViewModel.HasSelectedIpAddresses, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <controls:WrapPanel HorizontalSpacing="8"
                                                                    Orientation="Horizontal"
                                                                    VerticalSpacing="8" />
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                    </ItemsControl>
                                </ScrollViewer>
                            </Grid>

                        </controls:SettingsCard.Header>

                        <controls:SettingsCard.ActionIcon>
                            <pathicons:ChevronRight />
                        </controls:SettingsCard.ActionIcon>
                    </controls:SettingsCard>

                </StackPanel>

            </Grid>

        </StackPanel>
    </custom:SettingsPageContentControl>

</Page>