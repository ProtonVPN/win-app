<?xml version="1.0" encoding="utf-8" ?>
<!--
Copyright (c) 2023 Proton AG

This file is part of ProtonVPN.

ProtonVPN is free software: you can redistribute it and/or modify
it under the terms of the GNU General Public License as published by
the Free Software Foundation, either version 3 of the License, or
(at your option) any later version.

ProtonVPN is distributed in the hope that it will be useful,
but WITHOUT ANY WARRANTY; without even the implied warranty of
MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
GNU General Public License for more details.

You should have received a copy of the GNU General Public License
along with ProtonVPN.  If not, see <https://www.gnu.org/licenses/>.
-->

<Page x:Class="ProtonVPN.Client.UI.Home.HomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:pathicons="using:ProtonVPN.Client.Common.UI.Assets.Icons.PathIcons"
      xmlns:controls="using:ProtonVPN.Client.Common.UI.Controls"
      xmlns:helpers="using:ProtonVPN.Client.Helpers"
      xmlns:connectionCard="using:ProtonVPN.Client.UI.Home.ConnectionCard"
      xmlns:map="using:ProtonVPN.Client.UI.Home.Map"
      xmlns:recents="using:ProtonVPN.Client.UI.Home.Recents"
      xmlns:vpnStatusComponent="using:ProtonVPN.Client.UI.Home.VpnStatusComponent"
      mc:Ignorable="d">

    <Page.Resources>
        <ResourceDictionary>
            <x:Double x:Key="DetailsPaneWidth">260</x:Double>

            <helpers:SliderValueToMarginConverter x:Key="SliderValueToMarginConverter"
                                                  MaximumValue="{StaticResource MaximumDetailsPaneWidth}"
                                                  MinimumValue="{StaticResource MinimumDetailsPaneWidth}" />

            <x:Double x:Key="MapOverlayGradientHeight">82.0</x:Double>

            <x:Double x:Key="MaximumContentWidth">600.0</x:Double>
        </ResourceDictionary>
    </Page.Resources>

    <Grid x:Name="ContentArea">
        <SplitView x:Name="SplitViewArea"
                   BorderThickness="0"
                   CornerRadius="4,0,0,4"
                   IsPaneOpen="{x:Bind ViewModel.IsDetailsPaneOpen, Mode=TwoWay}"
                   DisplayMode="{x:Bind ViewModel.DetailsPaneDisplayMode, Mode=TwoWay}"
                   OpenPaneLength="{StaticResource DetailsPaneWidth}"
                   PaneBackground="{StaticResource PaneOverlayBackgroundBrush}"
                   PanePlacement="Right">
            <SplitView.Pane>
                <Grid Name="PaneGrid">
                    <Grid Margin="10,0,0,0"
                          Padding="14,24,24,24"
                          Background="Transparent">
                        <TextBlock HorizontalAlignment="Left"
                                   VerticalAlignment="Top"
                                   Text="Connection Details" />

                        <Button Padding="4"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Top"
                                AutomationProperties.AutomationId="CloseDetailsPaneButton"
                                Background="Transparent"
                                BorderBrush="Transparent"
                                Command="{x:Bind ViewModel.CloseDetailsPaneCommand}">
                            <pathicons:Cross />
                        </Button>
                    </Grid>
                </Grid>
            </SplitView.Pane>

            <controls:PageContentControl x:Name="PageContainer"
                                         Padding="0,0,0,16"
                                         CanContentScroll="False"
                                         IsCompact="{x:Bind ViewModel.IsNavigationPaneCollapsed, Mode=OneWay}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0">

                        <map:MapComponent AutomationProperties.AutomationId="MapComponent" />

                        <vpnStatusComponent:VpnStatusComponent Grid.Row="0"
                                                               AutomationProperties.AutomationId="VpnStatusComponent" />

                        <Border Height="{StaticResource MapOverlayGradientHeight}"
                                VerticalAlignment="Bottom"
                                Background="{StaticResource ReverseGradientFadingColorBrush}" />
                    </Grid>

                    <connectionCard:ConnectionCardComponent Grid.Row="1"
                                                            MaxWidth="{StaticResource MaximumContentWidth}"
                                                            AutomationProperties.AutomationId="ConnectionCardComponent" />

                    <recents:RecentsComponent Grid.Row="2"
                                              MaxWidth="{StaticResource MaximumContentWidth}"
                                              AutomationProperties.AutomationId="RecentsComponent" />
                </Grid>
            </controls:PageContentControl>
        </SplitView>

        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="PaneDisplayModes">
                <VisualState x:Name="Inline">
                    <VisualState.StateTriggers>
                        <StateTrigger IsActive="{x:Bind ViewModel.IsDetailsPaneInline, Mode=OneWay}" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="SplitViewArea.PaneBackground" Value="Transparent" />
                        <Setter Target="SplitViewArea.CornerRadius" Value="0" />
                        <Setter Target="PageContainer.CornerRadius" Value="8,8,0,0" />
                        <Setter Target="PageContainer.BorderThickness" Value="1,1,1,0" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
            <VisualStateGroup x:Name="NavigationDisplayModes">
                <VisualState x:Name="Minimal">
                    <VisualState.StateTriggers>
                        <StateTrigger IsActive="{x:Bind ViewModel.IsNavigationPaneCollapsed, Mode=OneWay}" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="SplitViewArea.Margin" Value="0,45,0,0" />
                        <Setter Target="PageContainer.Margin" Value="0,-45,0,0" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Grid>
</Page>