<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ProtonVPN.Client.UI.Dialogs.Windows.ReportIssueShellPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ProtonVPN.Client.UI.Dialogs.Windows"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid x:Name="WindowTitleBar"
              Height="{StaticResource CompactTitleBarHeight}"
              Margin="{StaticResource CompactTitleBarMargin}"
              VerticalAlignment="Top"
              Canvas.ZIndex="1"
              IsHitTestVisible="True">
            <TextBlock x:Name="WindowTitleBarText"
                       VerticalAlignment="Center"
                       AutomationProperties.AutomationId="WindowTitleLabel"
                       Style="{StaticResource CaptionTextBlockStyle}"
                       TextWrapping="NoWrap" />
        </Grid>

        <Frame x:Name="NavigationFrame" Margin="-1,-1,0,0"/>
    </Grid>
</Page>
