<?xml version="1.0" encoding="utf-8" ?>
<!--
Copyright (c) 2023 Proton AG

This file is part of ProtonVPN.

ProtonVPN is free software: you can redistribute it and/or modify
it under the terms of the GNU General Public License as published by
the Free Software Foundation, either version 3 of the License, or
(at your option) any later version.

ProtonVPN is distributed in the hope that it will be useful,
but WITHOUT ANY WARRANTY; without even the implied warranty of
MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
GNU General Public License for more details.

You should have received a copy of the GNU General Public License
along with ProtonVPN.  If not, see <https://www.gnu.org/licenses/>.
-->

<UserControl x:Class="ProtonVPN.Client.Common.UI.Controls.PageContentControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:toolkit="using:CommunityToolkit.WinUI.UI.Controls"
             xmlns:pathicons="using:ProtonVPN.Client.Common.UI.Assets.Icons.PathIcons"
             x:DefaultBindMode="OneWay"
             mc:Ignorable="d">

    <Grid x:Name="PageContainer"
          Padding="{x:Bind Padding}"
          Background="{x:Bind Background}"
          BorderBrush="{x:Bind BorderBrush}"
          BorderThickness="{x:Bind BorderThickness}"
          CornerRadius="{x:Bind CornerRadius}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <toolkit:DockPanel x:Name="PART_HeaderContainer"
                           Grid.Row="0"
                           Margin="0,16"
                           Visibility="Collapsed">
            <Button Margin="0,4,16,0"
                    toolkit:DockPanel.Dock="Left"
                    AutomationProperties.AutomationId="GoBackButton"
                    Style="{StaticResource TertiaryButtonStyle}"
                    Command="{x:Bind BackCommand}"
                    Visibility="{Binding IsEnabled, RelativeSource={RelativeSource Mode=Self}}">
                <pathicons:ArrowLeft />
            </Button>
            <ContentPresenter x:Name="PART_HeaderPresenter"
                              AutomationProperties.AutomationId="PageTitleLabel"
                              Content="{x:Bind PageHeader}"
                              ContentTemplate="{x:Bind PageHeaderTemplate}"
                              FontSize="{ThemeResource TitleFontSize}"
                              FontWeight="{ThemeResource TitleFontWeight}"
                              LineHeight="{ThemeResource TitleLineHeight}"
                              OpticalMarginAlignment="TrimSideBearings" />
        </toolkit:DockPanel>

        <ScrollViewer Grid.Row="1"
                      Margin="0,0,-12,0"
                      Padding="0,0,12,0"
                      HorizontalScrollBarVisibility="Disabled"
                      VerticalScrollBarVisibility="{x:Bind CanContentScroll, Converter={StaticResource BooleanToScrollBarVisibilityConverter}}">

            <ContentPresenter Content="{x:Bind PageContent}"
                              ContentTemplate="{x:Bind PageContentTemplate}" />
        </ScrollViewer>

        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState x:Name="CompactMode">
                    <VisualState.StateTriggers>
                        <StateTrigger IsActive="{x:Bind IsCompact}"/>
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="PageContainer.CornerRadius" Value="0" />
                        <Setter Target="PageContainer.BorderThickness" Value="0,1,0,0" />
                        <Setter Target="PageContainer.Margin" Value="0,45,0,0" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="StandardMode"/>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Grid>
</UserControl>