<?xml version="1.0" encoding="utf-8" ?>
<!--
Copyright (c) 2024 Proton AG

This file is part of ProtonVPN.

ProtonVPN is free software: you can redistribute it and/or modify
it under the terms of the GNU General Public License as published by
the Free Software Foundation, either version 3 of the License, or
(at your option) any later version.

ProtonVPN is distributed in the hope that it will be useful,
but WITHOUT ANY WARRANTY; without even the implied warranty of
MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
GNU General Public License for more details.

You should have received a copy of the GNU General Public License
along with ProtonVPN.  If not, see <https://www.gnu.org/licenses/>.-->

<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:custom="using:ProtonVPN.Client.Common.UI.Controls.Custom"
                    xmlns:toolkit="using:CommunityToolkit.WinUI.Controls"
                    xmlns:triggers="using:CommunityToolkit.WinUI" 
                    xmlns:attached="using:ProtonVPN.Client.Common.UI.AttachedProperties">

    <ResourceDictionary.ThemeDictionaries>

        <ResourceDictionary x:Key="Light">

            <StaticResource x:Key="SettingsCardBackground" ResourceKey="InteractionDefaultColorBrush" />
            <StaticResource x:Key="SettingsCardBackgroundPointerOver" ResourceKey="InteractionDefaultHoverColorBrush" />
            <StaticResource x:Key="SettingsCardBackgroundPressed" ResourceKey="InteractionDefaultActiveColorBrush" />
            <StaticResource x:Key="SettingsCardBackgroundDisabled" ResourceKey="InteractionDefaultColorBrush" />

            <StaticResource x:Key="SettingsCardForeground" ResourceKey="TextNormColorBrush" />
            <StaticResource x:Key="SettingsCardForegroundPointerOver" ResourceKey="TextNormColorBrush" />
            <StaticResource x:Key="SettingsCardForegroundPressed" ResourceKey="TextNormColorBrush" />
            <StaticResource x:Key="SettingsCardForegroundDisabled" ResourceKey="TextDisabledColorBrush" />

            <StaticResource x:Key="SettingsCardContentForeground" ResourceKey="TextWeakColorBrush" />
            <StaticResource x:Key="SettingsCardDescriptionForeground" ResourceKey="TextWeakColorBrush" />

            <StaticResource x:Key="SettingsCardBorderBrush" ResourceKey="InteractionDefaultEmptyColorBrush" />
            <StaticResource x:Key="SettingsCardBorderBrushPointerOver" ResourceKey="InteractionDefaultEmptyColorBrush" />
            <StaticResource x:Key="SettingsCardBorderBrushPressed" ResourceKey="InteractionDefaultEmptyColorBrush" />
            <StaticResource x:Key="SettingsCardBorderBrushDisabled" ResourceKey="InteractionDefaultEmptyColorBrush" />
        </ResourceDictionary>

        <ResourceDictionary x:Key="Dark">

            <StaticResource x:Key="SettingsCardBackground" ResourceKey="InteractionDefaultColorBrush" />
            <StaticResource x:Key="SettingsCardBackgroundPointerOver" ResourceKey="InteractionDefaultHoverColorBrush" />
            <StaticResource x:Key="SettingsCardBackgroundPressed" ResourceKey="InteractionDefaultActiveColorBrush" />
            <StaticResource x:Key="SettingsCardBackgroundDisabled" ResourceKey="InteractionDefaultColorBrush" />

            <StaticResource x:Key="SettingsCardForeground" ResourceKey="TextNormColorBrush" />
            <StaticResource x:Key="SettingsCardForegroundPointerOver" ResourceKey="TextNormColorBrush" />
            <StaticResource x:Key="SettingsCardForegroundPressed" ResourceKey="TextNormColorBrush" />
            <StaticResource x:Key="SettingsCardForegroundDisabled" ResourceKey="TextDisabledColorBrush" />

            <StaticResource x:Key="SettingsCardContentForeground" ResourceKey="TextWeakColorBrush" />
            <StaticResource x:Key="SettingsCardDescriptionForeground" ResourceKey="TextWeakColorBrush" />

            <StaticResource x:Key="SettingsCardBorderBrush" ResourceKey="InteractionDefaultEmptyColorBrush" />
            <StaticResource x:Key="SettingsCardBorderBrushPointerOver" ResourceKey="InteractionDefaultEmptyColorBrush" />
            <StaticResource x:Key="SettingsCardBorderBrushPressed" ResourceKey="InteractionDefaultEmptyColorBrush" />
            <StaticResource x:Key="SettingsCardBorderBrushDisabled" ResourceKey="InteractionDefaultEmptyColorBrush" />
        </ResourceDictionary>
    </ResourceDictionary.ThemeDictionaries>

    <Thickness x:Key="SettingsCardBorderThickness">1</Thickness>
    <Thickness x:Key="SettingsCardPadding">16,12</Thickness>
    <Thickness x:Key="GridSettingsCardPadding">16</Thickness>
    <x:Double x:Key="SettingsCardMinWidth">120</x:Double>
    <x:Double x:Key="SettingsCardMinHeight">54</x:Double>
    <x:Double x:Key="SettingsCardDescriptionFontSize">12</x:Double>
    <x:Double x:Key="SettingsCardHeaderIconMaxSize">20</x:Double>
    <x:Double x:Key="SettingsCardLeftIndention">0</x:Double>
    <x:Double x:Key="SettingsCardContentMinWidth">120</x:Double>
    <Thickness x:Key="SettingsCardHeaderIconMargin">0,0,12,0</Thickness>
    <Thickness x:Key="SettingsCardActionIconMargin">14,0,0,0</Thickness>
    <x:Double x:Key="SettingsCardActionIconMaxSize">16</x:Double>
    <Thickness x:Key="SettingsCardVerticalHeaderContentSpacing">0,8,0,0</Thickness>
    <VerticalAlignment x:Key="SettingsCardHeaderIconVerticalAlignment">Center</VerticalAlignment>
    <VerticalAlignment x:Key="SettingsCardHeaderVerticalAlignment">Center</VerticalAlignment>
    <FontWeight x:Key="SettingsCardHeaderFontWeight">Normal</FontWeight>

    <x:Double x:Key="SettingsCardWrapThreshold">260</x:Double>
    <x:Double x:Key="SettingsCardWrapNoIconThreshold">160</x:Double>

    <x:Double x:Key="FeatureSettingsCardHeaderIconMaxSize">32</x:Double>
    <Thickness x:Key="FeatureSettingsCardHeaderIconMargin">-4,0,12,0</Thickness>

    <Style x:Key="DefaultSettingsCardStyle" TargetType="toolkit:SettingsCard">
        <Setter Property="Background" Value="{ThemeResource SettingsCardBackground}" />
        <Setter Property="Foreground" Value="{ThemeResource SettingsCardForeground}" />
        <Setter Property="BorderBrush" Value="{ThemeResource SettingsCardBorderBrush}" />
        <Setter Property="BorderThickness" Value="{ThemeResource SettingsCardBorderThickness}" />
        <Setter Property="CornerRadius" Value="{ThemeResource ControlCornerRadius}" />
        <Setter Property="MinHeight" Value="{StaticResource SettingsCardMinHeight}" />
        <Setter Property="MinWidth" Value="{StaticResource SettingsCardMinWidth}" />
        <Setter Property="IsTabStop" Value="True" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="HorizontalContentAlignment" Value="Right" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="BackgroundSizing" Value="OuterBorderEdge" />
        <Setter Property="Padding" Value="{StaticResource SettingsCardPadding}" />
        <Setter Property="FontFamily" Value="{StaticResource ContentControlThemeFontFamily}" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="FontSize" Value="{StaticResource ControlContentThemeFontSize}" />
        <Setter Property="UseSystemFocusVisuals" Value="{StaticResource UseSystemFocusVisuals}" />
        <Setter Property="FocusVisualMargin" Value="-3" />
        <Setter Property="Margin" Value="0,4" />
        <Setter Property="attached:CustomTags.IsTagged" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="toolkit:SettingsCard">
                    <Grid x:Name="PART_RootGrid"
                          MinWidth="{TemplateBinding MinWidth}"
                          MinHeight="{TemplateBinding MinHeight}"
                          MaxWidth="{TemplateBinding MaxWidth}"
                          Padding="{TemplateBinding Padding}"
                          HorizontalAlignment="{TemplateBinding HorizontalAlignment}"
                          VerticalAlignment="{TemplateBinding VerticalAlignment}"
                          Background="{TemplateBinding Background}"
                          BackgroundSizing="{TemplateBinding BackgroundSizing}"
                          BorderBrush="{TemplateBinding BorderBrush}"
                          BorderThickness="{TemplateBinding BorderThickness}"
                          CornerRadius="{TemplateBinding CornerRadius}">
                        
                        <Grid.BackgroundTransition>
                            <BrushTransition Duration="0:0:0.083" />
                        </Grid.BackgroundTransition>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"
                                              MinWidth="{StaticResource SettingsCardLeftIndention}" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <Viewbox x:Name="PART_HeaderIconPresenterHolder"
                                 Grid.RowSpan="2"
                                 MaxWidth="{StaticResource SettingsCardHeaderIconMaxSize}"
                                 MaxHeight="{StaticResource SettingsCardHeaderIconMaxSize}"
                                 Margin="{StaticResource SettingsCardHeaderIconMargin}"
                                 VerticalAlignment="{ThemeResource SettingsCardHeaderIconVerticalAlignment}">
                            <ContentPresenter x:Name="PART_HeaderIconPresenter"
                                              AutomationProperties.AccessibilityView="Raw"
                                              Content="{TemplateBinding HeaderIcon}"
                                              HighContrastAdjustment="None" />
                        </Viewbox>

                        <StackPanel x:Name="HeaderPanel"
                                    Grid.Column="1"
                                    Margin="0,0,24,0"
                                    VerticalAlignment="{ThemeResource SettingsCardHeaderVerticalAlignment}"
                                    Orientation="Vertical">

                            <Grid HorizontalAlignment="Left"
                                  VerticalAlignment="Top">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <ContentPresenter x:Name="PART_HeaderPresenter"
                                                  Grid.Column="0"
                                                  Content="{TemplateBinding Header}"
                                                  FontSize="{StaticResource BodyFontSize}"
                                                  FontWeight="{ThemeResource SettingsCardHeaderFontWeight}"
                                                  HighContrastAdjustment="None"
                                                  TextWrapping="Wrap" />

                                <custom:TagControl Text="{Binding (attached:CustomTags.Tag), RelativeSource={RelativeSource TemplatedParent}, TargetNullValue='NEW'}" 
                                                   Style="{StaticResource NewTagControlStyle}"
                                                   Grid.Column="1"
                                                   Margin="8,0,4,2"
                                                   VerticalAlignment="Top"
                                                   Visibility="{Binding (attached:CustomTags.IsTagged), RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                            </Grid>

                            <ContentPresenter x:Name="PART_DescriptionPresenter"
                                              Content="{TemplateBinding Description}"
                                              FontSize="{StaticResource SettingsCardDescriptionFontSize}"
                                              Foreground="{ThemeResource SettingsCardDescriptionForeground}"
                                              TextWrapping="Wrap"
                                              Visibility="{Binding Description, RelativeSource={RelativeSource Mode=TemplatedParent}, Converter={StaticResource NotNullToVisibilityConverter}}" />
                        </StackPanel>

                        <ContentPresenter x:Name="PART_ContentPresenter"
                                          Grid.Column="2"
                                          HorizontalAlignment="Right"
                                          VerticalAlignment="Center"
                                          HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          Content="{TemplateBinding Content}"
                                          Foreground="{ThemeResource SettingsCardContentForeground}" />

                        <Viewbox x:Name="PART_ActionIconPresenterHolder"
                                 Grid.RowSpan="2"
                                 Grid.Column="3"
                                 MaxWidth="{StaticResource SettingsCardActionIconMaxSize}"
                                 MaxHeight="{StaticResource SettingsCardActionIconMaxSize}"
                                 Margin="{StaticResource SettingsCardActionIconMargin}"
                                 HorizontalAlignment="Center"
                                 VerticalAlignment="Center"
                                 HighContrastAdjustment="None"
                                 Visibility="{Binding IsClickEnabled, RelativeSource={RelativeSource Mode=TemplatedParent}, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <ContentPresenter x:Name="PART_ActionIconPresenter"
                                              AutomationProperties.AccessibilityView="Raw"
                                              Content="{TemplateBinding ActionIcon}"
                                              FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                              Foreground="{ThemeResource SettingsCardContentForeground}"
                                              HighContrastAdjustment="None"
                                              ToolTipService.ToolTip="{TemplateBinding ActionIconToolTip}" />
                        </Viewbox>
                        
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal" />
                                <VisualState x:Name="PointerOver">
                                    <VisualState.Setters>
                                        <Setter Target="PART_RootGrid.Background" Value="{ThemeResource SettingsCardBackgroundPointerOver}" />
                                        <Setter Target="PART_RootGrid.BorderBrush" Value="{ThemeResource SettingsCardBorderBrushPointerOver}" />
                                        <Setter Target="PART_HeaderPresenter.Foreground" Value="{ThemeResource SettingsCardForegroundPointerOver}" />
                                        <Setter Target="PART_HeaderIconPresenter.Foreground" Value="{ThemeResource SettingsCardForegroundPointerOver}" />
                                        <Setter Target="PART_ActionIconPresenter.Foreground" Value="{ThemeResource SettingsCardForegroundPointerOver}" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Pressed">
                                    <VisualState.Setters>
                                        <Setter Target="PART_RootGrid.Background" Value="{ThemeResource SettingsCardBackgroundPressed}" />
                                        <Setter Target="PART_RootGrid.BorderBrush" Value="{ThemeResource SettingsCardBorderBrushPressed}" />
                                        <Setter Target="PART_HeaderPresenter.Foreground" Value="{ThemeResource SettingsCardForegroundPressed}" />
                                        <Setter Target="PART_HeaderIconPresenter.Foreground" Value="{ThemeResource SettingsCardForegroundPressed}" />
                                        <Setter Target="PART_ActionIconPresenter.Foreground" Value="{ThemeResource SettingsCardForegroundPressed}" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Disabled">
                                    <VisualState.Setters>
                                        <Setter Target="PART_RootGrid.Background" Value="{ThemeResource SettingsCardBackgroundDisabled}" />
                                        <Setter Target="PART_RootGrid.BorderBrush" Value="{ThemeResource SettingsCardBorderBrushDisabled}" />
                                        <Setter Target="PART_ActionIconPresenter.Foreground" Value="{ThemeResource SettingsCardForegroundDisabled}" />
                                        <Setter Target="PART_DescriptionPresenter.Foreground" Value="{ThemeResource SettingsCardForegroundDisabled}" />
                                        <Setter Target="PART_HeaderPresenter.Foreground" Value="{ThemeResource SettingsCardForegroundDisabled}" />
                                        <Setter Target="PART_HeaderIconPresenter.Foreground" Value="{ThemeResource SettingsCardForegroundDisabled}" />
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>

                            <VisualStateGroup x:Name="ContentAlignmentStates">
                                <VisualState x:Name="Right" />

                                <VisualState x:Name="RightWrapped">
                                    <VisualState.StateTriggers>
                                        <triggers:ControlSizeTrigger MinWidth="{ThemeResource SettingsCardWrapNoIconThreshold}"
                                                                     MaxWidth="{ThemeResource SettingsCardWrapThreshold}"
                                                                     TargetElement="{Binding ElementName=PART_RootGrid}" />
                                    </VisualState.StateTriggers>
                                    <VisualState.Setters>
                                        <Setter Target="PART_ContentPresenter.(Grid.Row)" Value="1" />
                                        <Setter Target="PART_ContentPresenter.(Grid.Column)" Value="1" />
                                        <Setter Target="PART_ContentPresenter.HorizontalAlignment" Value="Stretch" />
                                        <Setter Target="PART_ContentPresenter.HorizontalContentAlignment" Value="Left" />
                                        <Setter Target="PART_ContentPresenter.Margin" Value="{StaticResource SettingsCardVerticalHeaderContentSpacing}" />
                                        <Setter Target="HeaderPanel.Margin" Value="0" />
                                    </VisualState.Setters>
                                </VisualState>

                                <VisualState x:Name="RightWrappedNoIcon">
                                    <VisualState.StateTriggers>
                                        <triggers:ControlSizeTrigger MaxWidth="{ThemeResource SettingsCardWrapNoIconThreshold}"
                                                                     TargetElement="{Binding ElementName=PART_RootGrid}" />
                                    </VisualState.StateTriggers>
                                    <VisualState.Setters>
                                        <Setter Target="PART_HeaderIconPresenterHolder.Visibility" Value="Collapsed" />
                                        <Setter Target="PART_ContentPresenter.(Grid.Row)" Value="1" />
                                        <Setter Target="PART_ContentPresenter.(Grid.Column)" Value="1" />
                                        <Setter Target="PART_ContentPresenter.HorizontalAlignment" Value="Stretch" />
                                        <Setter Target="PART_ContentPresenter.HorizontalContentAlignment" Value="Left" />
                                        <Setter Target="PART_ContentPresenter.Margin" Value="{StaticResource SettingsCardVerticalHeaderContentSpacing}" />
                                        <Setter Target="HeaderPanel.Margin" Value="0" />
                                    </VisualState.Setters>
                                </VisualState>

                                <VisualState x:Name="Left">
                                    <VisualState.StateTriggers>
                                        <triggers:IsEqualStateTrigger Value="{Binding ContentAlignment, RelativeSource={RelativeSource TemplatedParent}}"
                                                                      To="Left" />
                                    </VisualState.StateTriggers>
                                    <VisualState.Setters>
                                        <Setter Target="PART_HeaderIconPresenterHolder.Visibility" Value="Collapsed" />
                                        <Setter Target="PART_DescriptionPresenter.Visibility" Value="Collapsed" />
                                        <Setter Target="PART_HeaderPresenter.Visibility" Value="Collapsed" />
                                        <Setter Target="PART_ContentPresenter.(Grid.Row)" Value="1" />
                                        <Setter Target="PART_ContentPresenter.(Grid.Column)" Value="1" />
                                        <Setter Target="PART_ContentPresenter.HorizontalAlignment" Value="Left" />
                                    </VisualState.Setters>
                                </VisualState>

                                <VisualState x:Name="Vertical">
                                    <VisualState.StateTriggers>
                                        <triggers:IsEqualStateTrigger Value="{Binding ContentAlignment, RelativeSource={RelativeSource TemplatedParent}}"
                                                                      To="Vertical" />
                                    </VisualState.StateTriggers>
                                    <VisualState.Setters>
                                        <Setter Target="PART_ContentPresenter.(Grid.Row)" Value="1" />
                                        <Setter Target="PART_ContentPresenter.(Grid.Column)" Value="1" />
                                        <Setter Target="PART_ContentPresenter.HorizontalAlignment" Value="Stretch" />
                                        <Setter Target="PART_ContentPresenter.HorizontalContentAlignment" Value="{Binding HorizontalContentAlignment, RelativeSource={RelativeSource TemplatedParent}}" />
                                        <Setter Target="PART_ContentPresenter.Margin" Value="{StaticResource SettingsCardVerticalHeaderContentSpacing}" />
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>

                            <VisualStateGroup x:Name="ContentVisibilityStates">
                                <VisualState x:Name="Visible" />
                                <VisualState x:Name="Collapsed">
                                    <VisualState.StateTriggers>
                                        <triggers:IsNullOrEmptyStateTrigger Value="{Binding Content, RelativeSource={RelativeSource TemplatedParent}}" />
                                    </VisualState.StateTriggers>
                                    <VisualState.Setters>
                                        <Setter Target="PART_ContentPresenter.Visibility" Value="Collapsed" />
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style BasedOn="{StaticResource DefaultSettingsCardStyle}" TargetType="toolkit:SettingsCard" />

    <Style x:Key="VerticalSettingsCardStyle" BasedOn="{StaticResource DefaultSettingsCardStyle}" TargetType="toolkit:SettingsCard">
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="toolkit:SettingsCard">
                    <Grid x:Name="PART_RootGrid"
                          MinWidth="{TemplateBinding MinWidth}"
                          MinHeight="{TemplateBinding MinHeight}"
                          MaxWidth="{TemplateBinding MaxWidth}"
                          Padding="{TemplateBinding Padding}"
                          HorizontalAlignment="{TemplateBinding HorizontalAlignment}"
                          VerticalAlignment="{TemplateBinding VerticalAlignment}"
                          Background="{TemplateBinding Background}"
                          BackgroundSizing="{TemplateBinding BackgroundSizing}"
                          BorderBrush="{TemplateBinding BorderBrush}"
                          BorderThickness="{TemplateBinding BorderThickness}"
                          CornerRadius="{TemplateBinding CornerRadius}">
                        <Grid.BackgroundTransition>
                            <BrushTransition Duration="0:0:0.083" />
                        </Grid.BackgroundTransition>
                        <Grid.RowDefinitions>
                            <RowDefinition x:Name="HeaderRowDefinition"
                                           Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"
                                              MinWidth="{ThemeResource SettingsCardLeftIndention}" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <Viewbox x:Name="PART_HeaderIconPresenterHolder"
                                 MaxWidth="{ThemeResource SettingsCardHeaderIconMaxSize}"
                                 MaxHeight="{ThemeResource SettingsCardHeaderIconMaxSize}"
                                 Margin="{ThemeResource SettingsCardHeaderIconMargin}">
                            <ContentPresenter x:Name="PART_HeaderIconPresenter"
                                              AutomationProperties.AccessibilityView="Raw"
                                              Content="{TemplateBinding HeaderIcon}"
                                              HighContrastAdjustment="None" />
                        </Viewbox>

                        <StackPanel x:Name="HeaderPanel"
                                    Grid.Column="1"
                                    Margin="0,0,24,0"
                                    VerticalAlignment="Center"
                                    Orientation="Vertical">

                            <ContentPresenter x:Name="PART_HeaderPresenter"
                                              HorizontalAlignment="Left"
                                              Content="{TemplateBinding Header}"
                                              FontSize="{StaticResource BodyFontSize}"
                                              FontWeight="{StaticResource BodyFontWeight}"
                                              HighContrastAdjustment="None"
                                              TextWrapping="Wrap" />

                            <ContentPresenter x:Name="PART_DescriptionPresenter"
                                              Content="{TemplateBinding Description}"
                                              FontSize="{ThemeResource SettingsCardDescriptionFontSize}"
                                              Foreground="{ThemeResource SettingsCardDescriptionForeground}"
                                              TextWrapping="Wrap"
                                              Visibility="{Binding Description, RelativeSource={RelativeSource Mode=TemplatedParent}, Converter={StaticResource NotNullToVisibilityConverter}}" />
                        </StackPanel>

                        <Viewbox x:Name="PART_ActionIconPresenterHolder"
                                 Grid.Column="2"
                                 MaxWidth="{ThemeResource SettingsCardActionIconMaxSize}"
                                 MaxHeight="{ThemeResource SettingsCardActionIconMaxSize}"
                                 Margin="{ThemeResource SettingsCardActionIconMargin}"
                                 HorizontalAlignment="Center"
                                 VerticalAlignment="Center"
                                 HighContrastAdjustment="None"
                                 Visibility="{Binding IsClickEnabled, RelativeSource={RelativeSource Mode=TemplatedParent}, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <ContentPresenter x:Name="PART_ActionIconPresenter"
                                              AutomationProperties.AccessibilityView="Raw"
                                              Content="{TemplateBinding ActionIcon}"
                                              FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                              Foreground="{ThemeResource SettingsCardContentForeground}"
                                              HighContrastAdjustment="None"
                                              ToolTipService.ToolTip="{TemplateBinding ActionIconToolTip}" />
                        </Viewbox>

                        <ContentPresenter x:Name="PART_ContentPresenter"
                                          Grid.Row="1"
                                          Grid.Column="0"
                                          Grid.ColumnSpan="3"
                                          Margin="0,16,0,8"
                                          HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          Content="{TemplateBinding Content}"
                                          Foreground="{ThemeResource SettingsCardContentForeground}" />

                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal" />
                                <VisualState x:Name="PointerOver">
                                    <VisualState.Setters>
                                        <Setter Target="PART_RootGrid.Background" Value="{ThemeResource SettingsCardBackgroundPointerOver}" />
                                        <Setter Target="PART_RootGrid.BorderBrush" Value="{ThemeResource SettingsCardBorderBrushPointerOver}" />
                                        <Setter Target="PART_HeaderPresenter.Foreground" Value="{ThemeResource SettingsCardForegroundPointerOver}" />
                                        <Setter Target="PART_HeaderIconPresenter.Foreground" Value="{ThemeResource SettingsCardForegroundPointerOver}" />
                                        <Setter Target="PART_ActionIconPresenter.Foreground" Value="{ThemeResource SettingsCardForegroundPointerOver}" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Pressed">
                                    <VisualState.Setters>
                                        <Setter Target="PART_RootGrid.Background" Value="{ThemeResource SettingsCardBackgroundPressed}" />
                                        <Setter Target="PART_RootGrid.BorderBrush" Value="{ThemeResource SettingsCardBorderBrushPressed}" />
                                        <Setter Target="PART_HeaderPresenter.Foreground" Value="{ThemeResource SettingsCardForegroundPressed}" />
                                        <Setter Target="PART_HeaderIconPresenter.Foreground" Value="{ThemeResource SettingsCardForegroundPressed}" />
                                        <Setter Target="PART_ActionIconPresenter.Foreground" Value="{ThemeResource SettingsCardForegroundPressed}" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Disabled">
                                    <VisualState.Setters>
                                        <Setter Target="PART_RootGrid.Background" Value="{ThemeResource SettingsCardBackgroundDisabled}" />
                                        <Setter Target="PART_RootGrid.BorderBrush" Value="{ThemeResource SettingsCardBorderBrushDisabled}" />
                                        <Setter Target="PART_ActionIconPresenter.Foreground" Value="{ThemeResource SettingsCardForegroundDisabled}" />
                                        <Setter Target="PART_DescriptionPresenter.Foreground" Value="{ThemeResource SettingsCardForegroundDisabled}" />
                                        <Setter Target="PART_HeaderPresenter.Foreground" Value="{ThemeResource SettingsCardForegroundDisabled}" />
                                        <Setter Target="PART_HeaderIconPresenter.Foreground" Value="{ThemeResource SettingsCardForegroundDisabled}" />
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>

                            <VisualStateGroup x:Name="ContentVisibilityStates">
                                <VisualState x:Name="Visible" />
                                <VisualState x:Name="Collapsed">
                                    <VisualState.StateTriggers>
                                        <triggers:IsNullOrEmptyStateTrigger Value="{Binding Content, RelativeSource={RelativeSource TemplatedParent}}" />
                                    </VisualState.StateTriggers>
                                    <VisualState.Setters>
                                        <Setter Target="PART_ContentPresenter.Visibility" Value="Collapsed" />
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="FeatureSettingsCardStyle" BasedOn="{StaticResource DefaultSettingsCardStyle}" TargetType="custom:FeatureSettingsCard">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="custom:FeatureSettingsCard">
                    <Grid x:Name="PART_RootGrid"
                          MinWidth="{TemplateBinding MinWidth}"
                          MinHeight="{TemplateBinding MinHeight}"
                          MaxWidth="{TemplateBinding MaxWidth}"
                          Padding="{TemplateBinding Padding}"
                          HorizontalAlignment="{TemplateBinding HorizontalAlignment}"
                          VerticalAlignment="{TemplateBinding VerticalAlignment}"
                          Background="{TemplateBinding Background}"
                          BackgroundSizing="{TemplateBinding BackgroundSizing}"
                          BorderBrush="{TemplateBinding BorderBrush}"
                          BorderThickness="{TemplateBinding BorderThickness}"
                          CornerRadius="{TemplateBinding CornerRadius}">
                        <Grid.BackgroundTransition>
                            <BrushTransition Duration="0:0:0.083" />
                        </Grid.BackgroundTransition>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"
                                              MinWidth="{StaticResource SettingsCardLeftIndention}" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <Viewbox x:Name="PART_HeaderIconPresenterHolder"
                                 Grid.RowSpan="2"
                                 MaxWidth="{StaticResource FeatureSettingsCardHeaderIconMaxSize}"
                                 MaxHeight="{StaticResource FeatureSettingsCardHeaderIconMaxSize}"
                                 Margin="{StaticResource FeatureSettingsCardHeaderIconMargin}">
                            <Grid>
                                <!--  PART_HeaderIconPresenter is referenced in the code behind and cannot be removed even though it isn't use in the FeatureSettingsCard  -->
                                <ContentPresenter x:Name="PART_HeaderIconPresenter"
                                                  AutomationProperties.AccessibilityView="Raw"
                                                  Content="{TemplateBinding HeaderIcon}"
                                                  HighContrastAdjustment="None" />
                                <ContentPresenter x:Name="PART_FeatureIconPresenter"
                                                  AutomationProperties.AccessibilityView="Raw"
                                                  Content="{TemplateBinding FeatureIcon}"
                                                  HighContrastAdjustment="None" />
                            </Grid>
                        </Viewbox>

                        <StackPanel x:Name="HeaderPanel"
                                    Grid.Column="1"
                                    Margin="0,0,24,0"
                                    VerticalAlignment="Center"
                                    Orientation="Vertical">

                            <ContentPresenter x:Name="PART_HeaderPresenter"
                                              HorizontalAlignment="Left"
                                              Content="{TemplateBinding Header}"
                                              FontSize="{StaticResource BodyFontSize}"
                                              FontWeight="{StaticResource BodyFontWeight}"
                                              HighContrastAdjustment="None"
                                              TextWrapping="Wrap" />

                            <ContentPresenter x:Name="PART_DescriptionPresenter"
                                              Content="{TemplateBinding Description}"
                                              FontSize="{StaticResource SettingsCardDescriptionFontSize}"
                                              Foreground="{ThemeResource SettingsCardDescriptionForeground}"
                                              TextWrapping="Wrap"
                                              Visibility="{Binding Description, RelativeSource={RelativeSource Mode=TemplatedParent}, Converter={StaticResource NotNullToVisibilityConverter}}" />
                        </StackPanel>

                        <ContentPresenter x:Name="PART_ContentPresenter"
                                          Grid.Column="2"
                                          HorizontalAlignment="Right"
                                          VerticalAlignment="Center"
                                          HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          Content="{TemplateBinding Content}"
                                          Foreground="{ThemeResource SettingsCardContentForeground}" />

                        <Viewbox x:Name="PART_ActionIconPresenterHolder"
                                 Grid.RowSpan="2"
                                 Grid.Column="3"
                                 MaxWidth="{StaticResource SettingsCardActionIconMaxSize}"
                                 MaxHeight="{StaticResource SettingsCardActionIconMaxSize}"
                                 Margin="{StaticResource SettingsCardActionIconMargin}"
                                 HorizontalAlignment="Center"
                                 VerticalAlignment="Center"
                                 HighContrastAdjustment="None"
                                 Visibility="{Binding IsClickEnabled, RelativeSource={RelativeSource Mode=TemplatedParent}, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <ContentPresenter x:Name="PART_ActionIconPresenter"
                                              AutomationProperties.AccessibilityView="Raw"
                                              Content="{TemplateBinding ActionIcon}"
                                              FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                              Foreground="{ThemeResource SettingsCardContentForeground}"
                                              HighContrastAdjustment="None"
                                              ToolTipService.ToolTip="{TemplateBinding ActionIconToolTip}" />
                        </Viewbox>
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal" />
                                <VisualState x:Name="PointerOver">
                                    <VisualState.Setters>
                                        <Setter Target="PART_RootGrid.Background" Value="{ThemeResource SettingsCardBackgroundPointerOver}" />
                                        <Setter Target="PART_RootGrid.BorderBrush" Value="{ThemeResource SettingsCardBorderBrushPointerOver}" />
                                        <Setter Target="PART_HeaderPresenter.Foreground" Value="{ThemeResource SettingsCardForegroundPointerOver}" />
                                        <Setter Target="PART_HeaderIconPresenter.Foreground" Value="{ThemeResource SettingsCardForegroundPointerOver}" />
                                        <Setter Target="PART_ActionIconPresenter.Foreground" Value="{ThemeResource SettingsCardForegroundPointerOver}" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Pressed">
                                    <VisualState.Setters>
                                        <Setter Target="PART_RootGrid.Background" Value="{ThemeResource SettingsCardBackgroundPressed}" />
                                        <Setter Target="PART_RootGrid.BorderBrush" Value="{ThemeResource SettingsCardBorderBrushPressed}" />
                                        <Setter Target="PART_HeaderPresenter.Foreground" Value="{ThemeResource SettingsCardForegroundPressed}" />
                                        <Setter Target="PART_HeaderIconPresenter.Foreground" Value="{ThemeResource SettingsCardForegroundPressed}" />
                                        <Setter Target="PART_ActionIconPresenter.Foreground" Value="{ThemeResource SettingsCardForegroundPressed}" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Disabled">
                                    <VisualState.Setters>
                                        <Setter Target="PART_RootGrid.Background" Value="{ThemeResource SettingsCardBackgroundDisabled}" />
                                        <Setter Target="PART_RootGrid.BorderBrush" Value="{ThemeResource SettingsCardBorderBrushDisabled}" />
                                        <Setter Target="PART_ActionIconPresenter.Foreground" Value="{ThemeResource SettingsCardForegroundDisabled}" />
                                        <Setter Target="PART_DescriptionPresenter.Foreground" Value="{ThemeResource SettingsCardForegroundDisabled}" />
                                        <Setter Target="PART_HeaderPresenter.Foreground" Value="{ThemeResource SettingsCardForegroundDisabled}" />
                                        <Setter Target="PART_HeaderIconPresenter.Foreground" Value="{ThemeResource SettingsCardForegroundDisabled}" />
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>

                            <VisualStateGroup x:Name="ContentAlignmentStates">
                                <VisualState x:Name="Right" />

                                <VisualState x:Name="RightWrapped">
                                    <VisualState.StateTriggers>
                                        <triggers:ControlSizeTrigger MinWidth="{ThemeResource SettingsCardWrapNoIconThreshold}"
                                                                     MaxWidth="{ThemeResource SettingsCardWrapThreshold}"
                                                                     TargetElement="{Binding ElementName=PART_RootGrid}" />
                                    </VisualState.StateTriggers>
                                    <VisualState.Setters>
                                        <Setter Target="PART_ContentPresenter.(Grid.Row)" Value="1" />
                                        <Setter Target="PART_ContentPresenter.(Grid.Column)" Value="1" />
                                        <Setter Target="PART_ContentPresenter.HorizontalAlignment" Value="Stretch" />
                                        <Setter Target="PART_ContentPresenter.HorizontalContentAlignment" Value="Left" />
                                        <Setter Target="PART_ContentPresenter.Margin" Value="{StaticResource SettingsCardVerticalHeaderContentSpacing}" />
                                        <Setter Target="HeaderPanel.Margin" Value="0" />
                                    </VisualState.Setters>
                                </VisualState>

                                <VisualState x:Name="RightWrappedNoIcon">
                                    <VisualState.StateTriggers>
                                        <triggers:ControlSizeTrigger MaxWidth="{ThemeResource SettingsCardWrapNoIconThreshold}"
                                                                     TargetElement="{Binding ElementName=PART_RootGrid}" />
                                    </VisualState.StateTriggers>
                                    <VisualState.Setters>
                                        <Setter Target="PART_HeaderIconPresenterHolder.Visibility" Value="Collapsed" />
                                        <Setter Target="PART_ContentPresenter.(Grid.Row)" Value="1" />
                                        <Setter Target="PART_ContentPresenter.(Grid.Column)" Value="1" />
                                        <Setter Target="PART_ContentPresenter.HorizontalAlignment" Value="Stretch" />
                                        <Setter Target="PART_ContentPresenter.HorizontalContentAlignment" Value="Left" />
                                        <Setter Target="PART_ContentPresenter.Margin" Value="{StaticResource SettingsCardVerticalHeaderContentSpacing}" />
                                        <Setter Target="HeaderPanel.Margin" Value="0" />
                                    </VisualState.Setters>
                                </VisualState>

                                <VisualState x:Name="Left">
                                    <VisualState.StateTriggers>
                                        <triggers:IsEqualStateTrigger Value="{Binding ContentAlignment, RelativeSource={RelativeSource TemplatedParent}}"
                                                                      To="Left" />
                                    </VisualState.StateTriggers>
                                    <VisualState.Setters>
                                        <Setter Target="PART_HeaderIconPresenterHolder.Visibility" Value="Collapsed" />
                                        <Setter Target="PART_DescriptionPresenter.Visibility" Value="Collapsed" />
                                        <Setter Target="PART_HeaderPresenter.Visibility" Value="Collapsed" />
                                        <Setter Target="PART_ContentPresenter.(Grid.Row)" Value="1" />
                                        <Setter Target="PART_ContentPresenter.(Grid.Column)" Value="1" />
                                        <Setter Target="PART_ContentPresenter.HorizontalAlignment" Value="Left" />
                                    </VisualState.Setters>
                                </VisualState>

                                <VisualState x:Name="Vertical">
                                    <VisualState.StateTriggers>
                                        <triggers:IsEqualStateTrigger Value="{Binding ContentAlignment, RelativeSource={RelativeSource TemplatedParent}}"
                                                                      To="Vertical" />
                                    </VisualState.StateTriggers>
                                    <VisualState.Setters>
                                        <Setter Target="PART_ContentPresenter.(Grid.Row)" Value="1" />
                                        <Setter Target="PART_ContentPresenter.(Grid.Column)" Value="1" />
                                        <Setter Target="PART_ContentPresenter.HorizontalAlignment" Value="Stretch" />
                                        <Setter Target="PART_ContentPresenter.HorizontalContentAlignment" Value="{Binding HorizontalContentAlignment, RelativeSource={RelativeSource TemplatedParent}}" />
                                        <Setter Target="PART_ContentPresenter.Margin" Value="{StaticResource SettingsCardVerticalHeaderContentSpacing}" />
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>

                            <VisualStateGroup x:Name="ContentVisibilityStates">
                                <VisualState x:Name="Visible" />
                                <VisualState x:Name="Collapsed">
                                    <VisualState.StateTriggers>
                                        <triggers:IsNullOrEmptyStateTrigger Value="{Binding Content, RelativeSource={RelativeSource TemplatedParent}}" />
                                    </VisualState.StateTriggers>
                                    <VisualState.Setters>
                                        <Setter Target="PART_ContentPresenter.Visibility" Value="Collapsed" />
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="GhostSettingsCardStyle" BasedOn="{StaticResource DefaultSettingsCardStyle}" TargetType="toolkit:SettingsCard">
        <Setter Property="Background" Value="{ThemeResource TransparentColorBrush}" />
        <Setter Property="BorderBrush" Value="{ThemeResource TransparentColorBrush}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="CornerRadius" Value="{ThemeResource ControlCornerRadius}" />
        <Setter Property="MinHeight" Value="0" />
        <Setter Property="MinWidth" Value="0" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Margin" Value="0" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="toolkit:SettingsCard">
                    <Grid x:Name="PART_RootGrid"
                          MinWidth="{TemplateBinding MinWidth}"
                          MinHeight="{TemplateBinding MinHeight}"
                          MaxWidth="{TemplateBinding MaxWidth}"
                          Padding="{TemplateBinding Padding}"
                          HorizontalAlignment="{TemplateBinding HorizontalAlignment}"
                          VerticalAlignment="{TemplateBinding VerticalAlignment}"
                          Background="{TemplateBinding Background}"
                          BackgroundSizing="{TemplateBinding BackgroundSizing}"
                          BorderBrush="{TemplateBinding BorderBrush}"
                          BorderThickness="{TemplateBinding BorderThickness}"
                          CornerRadius="{TemplateBinding CornerRadius}">
                        <Grid.BackgroundTransition>
                            <BrushTransition Duration="0:0:0.083" />
                        </Grid.BackgroundTransition>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"
                                              MinWidth="{StaticResource SettingsCardLeftIndention}" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <Viewbox x:Name="PART_HeaderIconPresenterHolder"
                                 Grid.RowSpan="2"
                                 MaxWidth="{StaticResource SettingsCardHeaderIconMaxSize}"
                                 MaxHeight="{StaticResource SettingsCardHeaderIconMaxSize}"
                                 Margin="{StaticResource SettingsCardHeaderIconMargin}">
                            <ContentPresenter x:Name="PART_HeaderIconPresenter"
                                              AutomationProperties.AccessibilityView="Raw"
                                              Content="{TemplateBinding HeaderIcon}"
                                              HighContrastAdjustment="None" />
                        </Viewbox>

                        <StackPanel x:Name="HeaderPanel"
                                    Grid.Column="1"
                                    Margin="0,0,24,0"
                                    VerticalAlignment="Center"
                                    Orientation="Vertical">

                            <Grid HorizontalAlignment="Left"
                                  VerticalAlignment="Top">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <ContentPresenter x:Name="PART_HeaderPresenter"
                                                  Grid.Column="0"
                                                  Content="{TemplateBinding Header}"
                                                  FontSize="{StaticResource BodyFontSize}"
                                                  FontWeight="{ThemeResource SettingsCardHeaderFontWeight}"
                                                  HighContrastAdjustment="None"
                                                  TextWrapping="Wrap" />

                                <custom:TagControl Text="{Binding (attached:CustomTags.Tag), RelativeSource={RelativeSource TemplatedParent}, TargetNullValue='NEW'}" 
                                                   Style="{StaticResource NewTagControlStyle}"
                                                   Grid.Column="1"
                                                   Margin="8,0,4,2"
                                                   VerticalAlignment="Top"
                                                   Visibility="{Binding (attached:CustomTags.IsTagged), RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                            </Grid>

                            <ContentPresenter x:Name="PART_DescriptionPresenter"
                                              Content="{TemplateBinding Description}"
                                              FontSize="{StaticResource SettingsCardDescriptionFontSize}"
                                              Foreground="{ThemeResource SettingsCardDescriptionForeground}"
                                              TextWrapping="Wrap"
                                              Visibility="{Binding Description, RelativeSource={RelativeSource Mode=TemplatedParent}, Converter={StaticResource NotNullToVisibilityConverter}}" />
                        </StackPanel>

                        <ContentPresenter x:Name="PART_ContentPresenter"
                                          Grid.Column="2"
                                          HorizontalAlignment="Right"
                                          VerticalAlignment="Center"
                                          HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          Content="{TemplateBinding Content}"
                                          Foreground="{ThemeResource SettingsCardContentForeground}" />

                        <Viewbox x:Name="PART_ActionIconPresenterHolder"
                                 Grid.RowSpan="2"
                                 Grid.Column="3"
                                 MaxWidth="{StaticResource SettingsCardActionIconMaxSize}"
                                 MaxHeight="{StaticResource SettingsCardActionIconMaxSize}"
                                 Margin="{StaticResource SettingsCardActionIconMargin}"
                                 HorizontalAlignment="Center"
                                 VerticalAlignment="Center"
                                 HighContrastAdjustment="None"
                                 Visibility="{Binding IsClickEnabled, RelativeSource={RelativeSource Mode=TemplatedParent}, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <ContentPresenter x:Name="PART_ActionIconPresenter"
                                              AutomationProperties.AccessibilityView="Raw"
                                              Content="{TemplateBinding ActionIcon}"
                                              FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                              Foreground="{ThemeResource SettingsCardContentForeground}"
                                              HighContrastAdjustment="None"
                                              ToolTipService.ToolTip="{TemplateBinding ActionIconToolTip}" />
                        </Viewbox>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="InfoSettingsCardStyle" BasedOn="{StaticResource DefaultSettingsCardStyle}" TargetType="toolkit:SettingsCard">
        <Setter Property="Padding" Value="12" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="toolkit:SettingsCard">
                    <Grid x:Name="PART_RootGrid"
                          Padding="{TemplateBinding Padding}"
                          HorizontalAlignment="{TemplateBinding HorizontalAlignment}"
                          VerticalAlignment="{TemplateBinding VerticalAlignment}"
                          Background="{TemplateBinding Background}"
                          BackgroundSizing="{TemplateBinding BackgroundSizing}"
                          BorderBrush="{TemplateBinding BorderBrush}"
                          BorderThickness="{TemplateBinding BorderThickness}"
                          CornerRadius="{TemplateBinding CornerRadius}">
                        <Grid.BackgroundTransition>
                            <BrushTransition Duration="0:0:0.083" />
                        </Grid.BackgroundTransition>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"
                                              MinWidth="{StaticResource SettingsCardLeftIndention}" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <ContentPresenter x:Name="PART_HeaderIconPresenter"
                                          Grid.Row="0"
                                          Grid.Column="0"
                                          Margin="0,0,8,0"
                                          AutomationProperties.AccessibilityView="Raw"
                                          Content="{TemplateBinding HeaderIcon}"
                                          HighContrastAdjustment="None" />

                        <ContentPresenter x:Name="PART_HeaderPresenter"
                                          Grid.Row="0"
                                          Grid.Column="1"
                                          Margin="0,-2,0,0"
                                          HorizontalAlignment="Left"
                                          VerticalAlignment="Top"
                                          Content="{TemplateBinding Header}"
                                          FontSize="{StaticResource BodyMediumFontSize}"
                                          FontWeight="{StaticResource BodyMediumFontWeight}"
                                          HighContrastAdjustment="None"
                                          TextWrapping="Wrap" />

                        <ContentPresenter x:Name="PART_DescriptionPresenter"
                                          Grid.Row="1"
                                          Grid.Column="0"
                                          Grid.ColumnSpan="2"
                                          Margin="0,4,0,0"
                                          HorizontalAlignment="Left"
                                          VerticalAlignment="Top"
                                          Content="{TemplateBinding Description}"
                                          FontSize="{StaticResource BodyFontSize}"
                                          FontWeight="{StaticResource BodyFontWeight}"
                                          Foreground="{ThemeResource TextWeakColorBrush}"
                                          TextWrapping="Wrap"
                                          Visibility="{Binding Description, RelativeSource={RelativeSource Mode=TemplatedParent}, Converter={StaticResource NotNullToVisibilityConverter}}" />

                        <ContentPresenter x:Name="PART_ContentPresenter"
                                          Grid.Row="0"
                                          Grid.RowSpan="2"
                                          Grid.Column="2"
                                          HorizontalAlignment="Right"
                                          VerticalAlignment="Center"
                                          HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          Content="{TemplateBinding Content}"
                                          Foreground="{ThemeResource SettingsCardContentForeground}" />

                        <Viewbox x:Name="PART_ActionIconPresenterHolder"
                                 Grid.Row="0"
                                 Grid.RowSpan="2"
                                 Grid.Column="3"
                                 MaxWidth="{StaticResource SettingsCardActionIconMaxSize}"
                                 MaxHeight="{StaticResource SettingsCardActionIconMaxSize}"
                                 Margin="{StaticResource SettingsCardActionIconMargin}"
                                 HorizontalAlignment="Center"
                                 VerticalAlignment="Center"
                                 HighContrastAdjustment="None"
                                 Visibility="{Binding IsClickEnabled, RelativeSource={RelativeSource Mode=TemplatedParent}, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <ContentPresenter x:Name="PART_ActionIconPresenter"
                                              AutomationProperties.AccessibilityView="Raw"
                                              Content="{TemplateBinding ActionIcon}"
                                              FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                              Foreground="{ThemeResource SettingsCardContentForeground}"
                                              HighContrastAdjustment="None"
                                              ToolTipService.ToolTip="{TemplateBinding ActionIconToolTip}" />
                        </Viewbox>
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal" />
                                <VisualState x:Name="PointerOver">
                                    <VisualState.Setters>
                                        <Setter Target="PART_RootGrid.Background" Value="{ThemeResource SettingsCardBackgroundPointerOver}" />
                                        <Setter Target="PART_RootGrid.BorderBrush" Value="{ThemeResource SettingsCardBorderBrushPointerOver}" />
                                        <Setter Target="PART_HeaderPresenter.Foreground" Value="{ThemeResource SettingsCardForegroundPointerOver}" />
                                        <Setter Target="PART_HeaderIconPresenter.Foreground" Value="{ThemeResource SettingsCardForegroundPointerOver}" />
                                        <Setter Target="PART_ActionIconPresenter.Foreground" Value="{ThemeResource SettingsCardForegroundPointerOver}" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Pressed">
                                    <VisualState.Setters>
                                        <Setter Target="PART_RootGrid.Background" Value="{ThemeResource SettingsCardBackgroundPressed}" />
                                        <Setter Target="PART_RootGrid.BorderBrush" Value="{ThemeResource SettingsCardBorderBrushPressed}" />
                                        <Setter Target="PART_HeaderPresenter.Foreground" Value="{ThemeResource SettingsCardForegroundPressed}" />
                                        <Setter Target="PART_HeaderIconPresenter.Foreground" Value="{ThemeResource SettingsCardForegroundPressed}" />
                                        <Setter Target="PART_ActionIconPresenter.Foreground" Value="{ThemeResource SettingsCardForegroundPressed}" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Disabled">
                                    <VisualState.Setters>
                                        <Setter Target="PART_RootGrid.Background" Value="{ThemeResource SettingsCardBackgroundDisabled}" />
                                        <Setter Target="PART_RootGrid.BorderBrush" Value="{ThemeResource SettingsCardBorderBrushDisabled}" />
                                        <Setter Target="PART_ActionIconPresenter.Foreground" Value="{ThemeResource SettingsCardForegroundDisabled}" />
                                        <Setter Target="PART_DescriptionPresenter.Foreground" Value="{ThemeResource SettingsCardForegroundDisabled}" />
                                        <Setter Target="PART_HeaderPresenter.Foreground" Value="{ThemeResource SettingsCardForegroundDisabled}" />
                                        <Setter Target="PART_HeaderIconPresenter.Foreground" Value="{ThemeResource SettingsCardForegroundDisabled}" />
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>

                            <VisualStateGroup x:Name="ContentVisibilityStates">
                                <VisualState x:Name="Visible" />
                                <VisualState x:Name="Collapsed">
                                    <VisualState.StateTriggers>
                                        <triggers:IsNullOrEmptyStateTrigger Value="{Binding Content, RelativeSource={RelativeSource TemplatedParent}}" />
                                    </VisualState.StateTriggers>
                                    <VisualState.Setters>
                                        <Setter Target="PART_ContentPresenter.Visibility" Value="Collapsed" />
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="GridSettingsCardStyle" BasedOn="{StaticResource DefaultSettingsCardStyle}" TargetType="custom:GridSettingsCard">
        <Setter Property="Background" Value="{ThemeResource SettingsCardBackground}" />
        <Setter Property="Foreground" Value="{ThemeResource SettingsCardForeground}" />
        <Setter Property="BorderBrush" Value="{ThemeResource SettingsCardBorderBrush}" />
        <Setter Property="BorderThickness" Value="{ThemeResource SettingsCardBorderThickness}" />
        <Setter Property="CornerRadius" Value="{ThemeResource ControlCornerRadius}" />
        <Setter Property="MinHeight" Value="{StaticResource SettingsCardMinHeight}" />
        <Setter Property="MinWidth" Value="{StaticResource SettingsCardMinWidth}" />
        <Setter Property="IsTabStop" Value="True" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="HorizontalContentAlignment" Value="Right" />
        <Setter Property="VerticalAlignment" Value="Stretch" />
        <Setter Property="BackgroundSizing" Value="OuterBorderEdge" />
        <Setter Property="Padding" Value="{StaticResource SettingsCardPadding}" />
        <Setter Property="Padding" Value="{StaticResource GridSettingsCardPadding}" />
        <Setter Property="FontFamily" Value="{StaticResource ContentControlThemeFontFamily}" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="FontSize" Value="{StaticResource ControlContentThemeFontSize}" />
        <Setter Property="UseSystemFocusVisuals" Value="{StaticResource UseSystemFocusVisuals}" />
        <Setter Property="FocusVisualMargin" Value="-3" />
        <Setter Property="Margin" Value="0,4" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="custom:GridSettingsCard">
                    <Grid x:Name="PART_RootGrid"
                          MinWidth="{TemplateBinding MinWidth}"
                          MinHeight="{TemplateBinding MinHeight}"
                          MaxWidth="{TemplateBinding MaxWidth}"
                          Padding="{TemplateBinding Padding}"
                          HorizontalAlignment="{TemplateBinding HorizontalAlignment}"
                          VerticalAlignment="{TemplateBinding VerticalAlignment}"
                          Background="{TemplateBinding Background}"
                          BackgroundSizing="{TemplateBinding BackgroundSizing}"
                          BorderBrush="{TemplateBinding BorderBrush}"
                          BorderThickness="{TemplateBinding BorderThickness}"
                          CornerRadius="{TemplateBinding CornerRadius}"
                          RowSpacing="12">
                        <Grid.BackgroundTransition>
                            <BrushTransition Duration="0:0:0.083" />
                        </Grid.BackgroundTransition>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0">

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <Viewbox x:Name="PART_HeaderIconPresenterHolder"
                                     Grid.Column="0"
                                     MaxWidth="32"
                                     MaxHeight="32"
                                     VerticalAlignment="Top"
                                     HorizontalAlignment="Left">
                                <ContentPresenter x:Name="PART_HeaderIconPresenter"
                                                  AutomationProperties.AccessibilityView="Raw"
                                                  Content="{TemplateBinding FeatureIcon}"
                                                  HighContrastAdjustment="None" />
                            </Viewbox>

                            <Image Grid.Column="1"
                                   Height="20"
                                   HorizontalAlignment="Right"
                                   VerticalAlignment="Top"
                                   Source="{ThemeResource VpnPlusIllustrationSource}"
                                   Visibility="{Binding IsSubscriptionBadgeVisible, RelativeSource={RelativeSource Mode=TemplatedParent}, Converter={StaticResource BooleanToVisibilityConverter}}" />
                        </Grid>

                        <StackPanel x:Name="HeaderPanel"
                                    Grid.Row="1"
                                    Orientation="Vertical"
                                    VerticalAlignment="Bottom"
                                    Spacing="4">

                            <Grid HorizontalAlignment="Left"
                                  VerticalAlignment="Top">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <ContentPresenter x:Name="PART_HeaderPresenter"
                                                  Grid.Column="0"
                                                  Content="{TemplateBinding Header}"
                                                  FontSize="{StaticResource BodyFontSize}"
                                                  FontWeight="{ThemeResource BodyMediumStrongFontWeight}"
                                                  HighContrastAdjustment="None"
                                                  TextWrapping="Wrap" />

                                <custom:TagControl Text="{Binding (attached:CustomTags.Tag), RelativeSource={RelativeSource TemplatedParent}, TargetNullValue='NEW'}" 
                                                   Style="{StaticResource NewTagControlStyle}"
                                                   Grid.Column="1"
                                                   Margin="8,0,4,2"
                                                   VerticalAlignment="Top"
                                                   Visibility="{Binding (attached:CustomTags.IsTagged), RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                            </Grid>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <ContentPresenter x:Name="PART_DescriptionPresenter"
                                                  Grid.Column="0"
                                                  Content="{TemplateBinding Description}"
                                                  FontSize="{StaticResource SettingsCardDescriptionFontSize}"
                                                  Foreground="{ThemeResource SettingsCardDescriptionForeground}"
                                                  TextWrapping="Wrap"
                                                  Visibility="{Binding Description, RelativeSource={RelativeSource Mode=TemplatedParent}, Converter={StaticResource NotNullToVisibilityConverter}}" />

                                <Viewbox x:Name="PART_ActionIconPresenterHolder"
                                         Grid.Column="1"
                                         MaxWidth="{StaticResource SettingsCardActionIconMaxSize}"
                                         MaxHeight="{StaticResource SettingsCardActionIconMaxSize}"
                                         Margin="{StaticResource SettingsCardActionIconMargin}"
                                         HorizontalAlignment="Right"
                                         VerticalAlignment="Center"
                                         HighContrastAdjustment="None"
                                         Visibility="{Binding IsClickEnabled, RelativeSource={RelativeSource Mode=TemplatedParent}, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <ContentPresenter x:Name="PART_ActionIconPresenter"
                                                      AutomationProperties.AccessibilityView="Raw"
                                                      Content="{TemplateBinding ActionIcon}"
                                                      FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                                      Foreground="{ThemeResource SettingsCardContentForeground}"
                                                      HighContrastAdjustment="None"
                                                      ToolTipService.ToolTip="{TemplateBinding ActionIconToolTip}" />
                                </Viewbox>
                            </Grid>


                        </StackPanel>

                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal" />
                                <VisualState x:Name="PointerOver">
                                    <VisualState.Setters>
                                        <Setter Target="PART_RootGrid.Background" Value="{ThemeResource SettingsCardBackgroundPointerOver}" />
                                        <Setter Target="PART_RootGrid.BorderBrush" Value="{ThemeResource SettingsCardBorderBrushPointerOver}" />
                                        <Setter Target="PART_HeaderPresenter.Foreground" Value="{ThemeResource SettingsCardForegroundPointerOver}" />
                                        <Setter Target="PART_HeaderIconPresenter.Foreground" Value="{ThemeResource SettingsCardForegroundPointerOver}" />
                                        <Setter Target="PART_ActionIconPresenter.Foreground" Value="{ThemeResource SettingsCardForegroundPointerOver}" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Pressed">
                                    <VisualState.Setters>
                                        <Setter Target="PART_RootGrid.Background" Value="{ThemeResource SettingsCardBackgroundPressed}" />
                                        <Setter Target="PART_RootGrid.BorderBrush" Value="{ThemeResource SettingsCardBorderBrushPressed}" />
                                        <Setter Target="PART_HeaderPresenter.Foreground" Value="{ThemeResource SettingsCardForegroundPressed}" />
                                        <Setter Target="PART_HeaderIconPresenter.Foreground" Value="{ThemeResource SettingsCardForegroundPressed}" />
                                        <Setter Target="PART_ActionIconPresenter.Foreground" Value="{ThemeResource SettingsCardForegroundPressed}" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Disabled">
                                    <VisualState.Setters>
                                        <Setter Target="PART_RootGrid.Background" Value="{ThemeResource SettingsCardBackgroundDisabled}" />
                                        <Setter Target="PART_RootGrid.BorderBrush" Value="{ThemeResource SettingsCardBorderBrushDisabled}" />
                                        <Setter Target="PART_ActionIconPresenter.Foreground" Value="{ThemeResource SettingsCardForegroundDisabled}" />
                                        <Setter Target="PART_DescriptionPresenter.Foreground" Value="{ThemeResource SettingsCardForegroundDisabled}" />
                                        <Setter Target="PART_HeaderPresenter.Foreground" Value="{ThemeResource SettingsCardForegroundDisabled}" />
                                        <Setter Target="PART_HeaderIconPresenter.Foreground" Value="{ThemeResource SettingsCardForegroundDisabled}" />
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>

                            <VisualStateGroup x:Name="ContentAlignmentStates">
                                <VisualState x:Name="Right" />

                                <VisualState x:Name="RightWrapped">
                                    <VisualState.StateTriggers>
                                        <triggers:ControlSizeTrigger MinWidth="{ThemeResource SettingsCardWrapNoIconThreshold}"
                                                                     MaxWidth="{ThemeResource SettingsCardWrapThreshold}"
                                                                     TargetElement="{Binding ElementName=PART_RootGrid}" />
                                    </VisualState.StateTriggers>
                                    <VisualState.Setters>
                                        <Setter Target="PART_ContentPresenter.(Grid.Row)" Value="1" />
                                        <Setter Target="PART_ContentPresenter.(Grid.Column)" Value="1" />
                                        <Setter Target="PART_ContentPresenter.HorizontalAlignment" Value="Stretch" />
                                        <Setter Target="PART_ContentPresenter.HorizontalContentAlignment" Value="Left" />
                                        <Setter Target="PART_ContentPresenter.Margin" Value="{StaticResource SettingsCardVerticalHeaderContentSpacing}" />
                                        <Setter Target="HeaderPanel.Margin" Value="0" />
                                    </VisualState.Setters>
                                </VisualState>

                                <VisualState x:Name="RightWrappedNoIcon">
                                    <VisualState.StateTriggers>
                                        <triggers:ControlSizeTrigger MaxWidth="{ThemeResource SettingsCardWrapNoIconThreshold}"
                                                                     TargetElement="{Binding ElementName=PART_RootGrid}" />
                                    </VisualState.StateTriggers>
                                    <VisualState.Setters>
                                        <Setter Target="PART_HeaderIconPresenterHolder.Visibility" Value="Collapsed" />
                                        <Setter Target="PART_ContentPresenter.(Grid.Row)" Value="1" />
                                        <Setter Target="PART_ContentPresenter.(Grid.Column)" Value="1" />
                                        <Setter Target="PART_ContentPresenter.HorizontalAlignment" Value="Stretch" />
                                        <Setter Target="PART_ContentPresenter.HorizontalContentAlignment" Value="Left" />
                                        <Setter Target="PART_ContentPresenter.Margin" Value="{StaticResource SettingsCardVerticalHeaderContentSpacing}" />
                                        <Setter Target="HeaderPanel.Margin" Value="0" />
                                    </VisualState.Setters>
                                </VisualState>

                                <VisualState x:Name="Left">
                                    <VisualState.StateTriggers>
                                        <triggers:IsEqualStateTrigger Value="{Binding ContentAlignment, RelativeSource={RelativeSource TemplatedParent}}"
                                                                      To="Left" />
                                    </VisualState.StateTriggers>
                                    <VisualState.Setters>
                                        <Setter Target="PART_HeaderIconPresenterHolder.Visibility" Value="Collapsed" />
                                        <Setter Target="PART_DescriptionPresenter.Visibility" Value="Collapsed" />
                                        <Setter Target="PART_HeaderPresenter.Visibility" Value="Collapsed" />
                                        <Setter Target="PART_ContentPresenter.(Grid.Row)" Value="1" />
                                        <Setter Target="PART_ContentPresenter.(Grid.Column)" Value="1" />
                                        <Setter Target="PART_ContentPresenter.HorizontalAlignment" Value="Left" />
                                    </VisualState.Setters>
                                </VisualState>

                                <VisualState x:Name="Vertical">
                                    <VisualState.StateTriggers>
                                        <triggers:IsEqualStateTrigger Value="{Binding ContentAlignment, RelativeSource={RelativeSource TemplatedParent}}"
                                                                      To="Vertical" />
                                    </VisualState.StateTriggers>
                                    <VisualState.Setters>
                                        <Setter Target="PART_ContentPresenter.(Grid.Row)" Value="1" />
                                        <Setter Target="PART_ContentPresenter.(Grid.Column)" Value="1" />
                                        <Setter Target="PART_ContentPresenter.HorizontalAlignment" Value="Stretch" />
                                        <Setter Target="PART_ContentPresenter.HorizontalContentAlignment" Value="{Binding HorizontalContentAlignment, RelativeSource={RelativeSource TemplatedParent}}" />
                                        <Setter Target="PART_ContentPresenter.Margin" Value="{StaticResource SettingsCardVerticalHeaderContentSpacing}" />
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>

                            <VisualStateGroup x:Name="ContentVisibilityStates">
                                <VisualState x:Name="Visible" />
                                <VisualState x:Name="Collapsed">
                                    <VisualState.StateTriggers>
                                        <triggers:IsNullOrEmptyStateTrigger Value="{Binding Content, RelativeSource={RelativeSource TemplatedParent}}" />
                                    </VisualState.StateTriggers>
                                    <VisualState.Setters>
                                        <Setter Target="PART_ContentPresenter.Visibility" Value="Collapsed" />
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>